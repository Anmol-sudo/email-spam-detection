# ── Dependencies for running Spam_Mail_Prediction_using_Machine_Learning.ipynb ─
# Install these on top of requirements.txt when you want to retrain or re-run
# the analysis:   pip install -r requirements.txt -r requirements-notebook.txt
# The Streamlit app does NOT need any of them.

-r requirements.txt

matplotlib>=3.7          # Phase 1/3/4 charts
seaborn>=0.12            # heatmaps and styling
wordcloud>=1.9           # Phase 1 word clouds
imbalanced-learn>=0.11   # Phase 6 SMOTE
lime>=0.2                # Phase 9 explainability
jupyter>=1.0             # to run the notebook locally
